/**
 * 연필·휴지통 아이콘의 SVG path 원문 — DESIGN_SPEC 2-1 아이콘 표의 값 그대로.
 *
 * **왜 상수 하나로 모으나** (J1 품질 리뷰 Minor 1): 이 두 문자열은 "관리자가 아닌 응답에는
 * 연필·휴지통 마크업이 **아예 없다**"를 확인하는 **음성 단언**에 쓰인다(README 주의사항 7).
 * 음성 단언은 문자열이 틀려도 통과한다 — path 를 손보고 테스트의 복붙본을 안 고치면
 * `not.toContain` 은 여전히 초록이고, 정작 `display:none` 회귀를 잡아 줄 유일한 단언이
 * 조용히 무력해진다. 그래서 파일마다 적어 두던 리터럴을 여기 한 곳으로 모았다.
 *
 * 이 상수가 **실제 렌더와 같다는 보증**은 `components/icons.test.tsx` 가 진다 — 그쪽은
 * 렌더 결과의 `d` 속성과 이 값을 **양성**으로 맞춰 본다. 그래서 아이콘 path 를 바꾸면
 * icons.test.tsx 가 먼저 빨개지고, 여기를 고치면 음성 단언들도 새 값으로 함께 따라온다.
 * 두 쪽을 잇는 것이 이 파일의 존재 이유이므로, 음성 단언 쪽에만 리터럴을 되살리지 마라.
 *
 * 아이콘마다 path 는 여럿이지만(연필 2개·휴지통 3개) 여기 두는 것은 **그 아이콘에서만
 * 나오는 조각 하나씩**이다. `M4 6.5h16` 같은 짧은 직선은 다른 아이콘과 겹칠 수 있어
 * "렌더됐는가"의 지표가 되지 못한다.
 */

/** 연필의 몸통 — 이 문자열이 DOM 에 있으면 연필이 그려진 것이다. */
export const PENCIL_PATH = 'M4 20.5h4L20 8.5l-4-4L4 16.5v4z';

/** 휴지통의 통 — 뚜껑(`M4 6.5h16`)이 아니라 통이어야 다른 아이콘과 겹치지 않는다. */
export const TRASH_PATH = 'M6.5 6.5l1 13.5h9l1-13.5';
