# Discord 링크 자동 수집 출시 기록 — 2026-08-10

## 범위와 담당

- 운영 프로젝트: Supabase `jwpceskhyltegqbwlvav`, Vercel `favorite-site`
- 감시 범위: Discord channel `1536239187517247488` 한 곳
- 즉시 중지 담당: 저장소·Vercel·Supabase·Hermes 접근 권한을 가진 프로젝트 소유자
- 기록 시각대: Asia/Seoul (KST)

## 데이터베이스

- `0002`~`0005` 기존 계약 검사와 474개 bookmark read-only preflight를 실행했다.
- URL 문법 위반, canonical collision, URL/title/description 길이 위반은 모두 0건이었다.
- 적용 전 backup과 적용 후 schema snapshot은 로컬 암호화 사용자 영역
  `/Users/itconnect/.local/share/favorite/backups/2026-08-10-discord-ingest/`에 mode 0600으로 보관한다.
- 운영 migration은 `0001`~`0007`이 local/remote에서 일치한다. 훈련용 migration history는 모두 제거했다.
- 운영 Data API schema verifier 11종이 통과했고, 관리자 임시 세션으로 manual bookmark 생성·수정·공개
  read·통계·cleanup·invalid URL 거부를 smoke한 뒤 fixture 0건을 확인했다.
- clean local reset, SQL corpus, runtime ACL·role membership whitelist, two-connection concurrency,
  favicon fence·CAS, Cron 계약과 database lint가 통과했다.

## 앱과 비밀 경계

- Vercel Production·Preview에 runtime DSN과 HMAC secret을 각각 encrypted secret으로 저장했다.
- Hermes에는 HMAC secret만 있고 DB DSN은 없다. 앱에는 DB DSN과 별도 복사본의 HMAC secret이 있다.
- Supavisor DSN은 `sslmode=require&uselibpqcompat=true`, `gssencmode=disable`, 고정
  `application_name=discord_ingest_api` 계약으로 exact connection probe를 통과했다.
- rollback drill의 2026-08-10 16:45:49 KST forward deployment는
  `dpl_Cfb9ZG8weaHtRbwUYWeEjcsX1fpv`였다.
- 최신 `origin/main` 병합 뒤 2026-08-10 17:10:21 KST 최종 production deployment
  `dpl_FJWYB36SBwE4puEXf8pWTARmbhEY`가 commit `8a874d0`을 서빙함을 `/api/version`으로 확인했다.
- favicon provider 승인 반영 뒤 production deployment `dpl_ATnywSebDBjM8o5isoVFPGfAJrjh`가 commit
  `b248f05`를 `favorite.itconnect.dev`에서 서빙함을 `/api/version`으로 다시 확인했다.
- 운영 별칭의 unsigned ingest는 401, signed category read는 200과 58개 category를 반환했다.
- 프로젝트 소유자가 공개 bookmark hostname의 favicon provider 전송을 2026-08-10에 명시 승인했다.
  `DISCORD_FAVICON_PROVIDER_APPROVED=true`를 Production·Preview에 exact 값으로 설정했고, 새 production
  deployment 뒤 provider button과 server action이 승인 상태를 사용한다.

## Hermes 경계

- favorite profile은 `openai-codex/gpt-5.6-luna`, reasoning `high`다.
- MCP server key는 `favorite-ingest`이고 Discord platform에는 category read와 ingest 두 tool만 허용한다.
- trusted Discord message ID는 session ContextVar에서 읽어 tool middleware가 model argument를 덮어쓴다.
- text batching delay는 0, bot input은 차단, user/channel allowlist는 지정 채널·소유자 한 명으로 제한했다.
- everyone, role, user, replied-user mention은 모두 false다.
- MCP stdio child에는 profile secret store의 `DISCORD_INGEST_API_URL`과 `DISCORD_INGEST_HMAC_SECRET`만
  `${VAR}` 참조로 명시 전달한다. 실제 secret은 `config.yaml`에 복사하지 않는다.
- gateway는 launchd supervised 상태이며 전역 `require_mention=true`를 유지한다. 지정 channel
  `1536239187517247488`만 free-response로 활성화했다.
- 재시작 뒤 Discord 연결, MCP 두 tool 등록, MCP child의 두 runtime env 존재·길이, 실제 signed
  `favorite_list_categories` 호출을 확인했다.

## 실제 복구 훈련

### 앱 rollback

- 2026-08-10 KST에 당시 운영 deployment `dpl_4hMX4QsSHDNfoa6MdK7swYvw1a5R`에서 직전
  `dpl_CVbJCvKsNeQHunBgJMXDm1ZSpWEC`로 Vercel rollback을 실행했다.
- 운영 alias와 unsigned ingest 401을 확인했다.
- 현재 작업 트리를 새 production deployment로 빌드한 뒤
  `dpl_Cfb9ZG8weaHtRbwUYWeEjcsX1fpv`를 promote했다.
- production build, unsigned 401, signed category 200/58을 다시 확인했다.

### DB kill switch

- favorite gateway를 먼저 정지했다.
- `discord_ingest_runtime NOLOGIN` 설정 후 그 role의 session만 `pg_terminate_backend`로 종료했다.
- signed request는 고정 `503 {"error":"service_unavailable"}`, unsigned request는 계속 401이었다.
- role을 `LOGIN`으로 복구하자 첫 재시도에서 signed request가 200/58로 회복했다.
- 훈련용 두 migration의 remote history와 local file을 제거했고 `0001`~`0007` 일치를 재확인했다.

### MCP runtime 복구 — 2026-08-15

- 10:01~10:09 KST의 링크 저장 실패는 운영 API나 서명 오류가 아니라 Hermes Agent `0.18.2`의 장기 실행
  stdio MCP transport가 종료된 상태에서 복구되지 않은 것이 원인이었다. 당시 gateway 아래
  `favorite-ingest` child가 없었고 저장 요청에서 MCP tool 호출도 발생하지 않았다.
- Favorite gateway를 정지한 뒤 Hermes 상태를
  `/Users/itconnect/.hermes/backups/pre-update-2026-08-15-101333.zip`에 백업하고 Hermes Agent
  `0.20.1` (`2026.8.13`)로 업데이트했다. 재기동 뒤 `mcp_stdio_watchdog.py`와 repository-local `tsx`
  child, MCP 두 tool 등록, Discord 연결을 모두 확인했다.
- 실제 channel에 `https://ai.keywert.com/about`를 다시 전송해 category 조회와 ingest가 모두 실행되고
  `저장 완료: ai.keywert.com (검색·리서치)` 응답이 도착한 것을 확인했다.
- 복구 검증으로 실행 중인 `tsx` leaf만 종료했다. 다음 동일 URL 요청에서 gateway가 session-expired를
  감지해 transport를 재생성하고 한 번 재시도했으며, 새 watchdog/child가 생성되고
  `이미 저장된 링크입니다: ai.keywert.com (검색·리서치)`가 도착했다. gateway 자체 재시작은 없었다.

## guarded activation과 남은 관찰 게이트

- 프로젝트 소유자가 message `1536300620837953588`, `1536300648402784328`로 `naver.com`, `daum.net`을
  연속 전송했다. 두 메시지는 bot user가 아니라 managed bot role을 mention해 기존 mention gate에서
  무시됐다. 과거 메시지는 자동 재생되지 않으므로 exact Discord message ID로 수동 복구했고, 두 URL 모두
  서로 다른 bookmark/receipt로 `검색·리서치`에 저장했다. 이 복구는 새 free-response inbound E2E를
  대신하지 않으므로 다음 실제 사용자 URL 한 건의 자동 응답을 별도 확인한다.
- bot 결과 message `1536301963853955082`는 user/role mention 없이 전송됐다. literal user/role/everyone
  canary message `1536302781017882717`도 Discord 응답의 `mentions=[]`, `mention_roles=[]`,
  `mention_everyone=false`를 확인했다.
- 지정 channel만 free-response로 추가했다. 다른 channel의 `require_mention=true`, user/channel allowlist,
  bot input 차단, URL 없는 메시지 `NO_REPLY`는 유지한다.
- 공개 bookmark hostname의 favicon provider 전송 승인을 받고 Production·Preview flag와 production
  deployment까지 반영했다.
- guarded activation 뒤 24시간 동안 자동 행·receipt outcome·expired receipt oldest age·rate ring·DB session·
  favicon failure/orphan backlog를 확인하고 이상이 없을 때 정식 승인한다.

## 이상 시 순서

1. favorite gateway를 정지한다.
2. `discord_ingest_runtime`을 `NOLOGIN`으로 바꾸고 그 role의 session만 종료한다.
3. signed endpoint가 generic 503인지 확인한다.
4. 필요하면 직전 Vercel production deployment로 rollback한다.
5. schema/data는 되돌리거나 삭제하지 않고 조사한다.
