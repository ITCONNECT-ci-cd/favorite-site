# PRD — 사내 링크 대시보드

## 1. 배경과 문제

사내 구성원이 자주 쓰는 시스템 주소를 기억하지 못해 슬랙에 반복해서 묻습니다.
개인 브라우저 북마크는 공유되지 않고, 노션 문서는 검색이 느리고 파비콘이 없어 훑어 읽기가 어렵습니다.

**해결할 문제**: "그거 주소 뭐였지?"를 없앤다.

## 2. 사용자와 맥락

- 주 사용자: 사내 구성원(비개발자 포함). 하루 3~10회 들어와 10초 안에 링크 하나 누르고 나갑니다.
- 관리자: 1~2명. 링크 등록·분류·정리를 담당합니다.
- 데이터 규모: 현재 290개 / 10개 상위 카테고리. 계속 늘어납니다.
- 환경: 데스크톱 1440px 주력, 모바일 375px 지원.

## 3. 성공 기준

1. 처음 온 사람이 3초 안에 "여기 뭐가 있는지" 구조를 파악한다.
2. 재방문자가 **클릭 2회 이내**(또는 검색어 입력 → Enter)로 목표 링크에 도달한다.
3. 링크 이름을 정확히 모르고 "대충 이런 거"만 아는 상태에서도 찾아낸다.
4. 링크가 늘어나도 1~3이 유지된다.
5. 마우스 없이 키보드만으로 검색 → 이동이 가능하다.

### 하드 제약
- 1440×900 첫 화면에서 즐겨찾기와 현재 운영 중인 사이트를 빠르게 훑을 수 있다.
- 히어로 배너·일러스트·그라데이션·장식 아이콘·마케팅 카피 금지.
- 라이트 모드, 무채색 + 파비콘. 카테고리를 색으로만 구분하지 않는다.
- 항목 표시 정보: 파비콘 + 이름 + 한 줄 설명 + 주소 + **눈 아이콘 + 누적 클릭 수**.
- **모든 화면이 같은 카드를 씁니다.** 홈·카테고리 목록·즐겨찾기 전부 동일한 사각 카드 그리드이며, 행 목록 형식은 관리자 화면에서만 씁니다.

## 4. 정보 구조

3계층입니다.

```
0단  내 즐겨찾기           (관리자가 핀으로 담은 공용 큐레이션)
     ├ AI 소식
     ├ AI 서비스
     └ 업무용 서비스
1단  현재 운영 중인 사이트   (회사가 직접 운영하는 서비스 16개)
2단  분류별 전체 목록
```

**홈에는 0·1단만 둡니다.** 나머지는 좌측 사이드바로만 들어갑니다. 즐겨찾기는
`bookmarks.is_favorite`·`fav_order`에 저장되는 관리자 큐레이션 한 벌이며, 로그인 여부나 PC와
무관하게 모든 방문자에게 같은 목록이 보입니다. 핀과 순서 편집은 관리자에게만 노출됩니다.

### 카테고리 (현재 데이터)

| 상위 카테고리 | 하위 | 링크 수 |
|---|---|---|
| 현재 운영 중인 사이트 | — | 16 |
| AI 도구 모음 | 대화·검색 | 26 |
| AI 도구 모음 | 코딩 | 24 |
| AI 도구 모음 | 영상 | 25 |
| AI 도구 모음 | 이미지 | 14 |
| AI 도구 모음 | 논문 | 9 |
| AI 도구 모음 | 디자인 | 7 |
| AI 도구 모음 | 오디오 | 5 |
| AI 도구 모음 | 프롬프트 | 3 |
| AI 도구 모음 | 문서 | 3 |
| AI 도구 모음 | 그 외 | 2 |
| 마케팅 | — | 21 |
| 웹 도구 | — | 22 |
| 강의 및 출강 | — | 7 |
| 자사 포트폴리오 | — | 21 |
| UI/UX 디자인 | — | 13 |
| 참고자료 | 도구·서비스 | 30 |
| 참고자료 | 학습·리서치 | 17 |
| 구글 서비스 | — | 18 |
| 기타 | — | 7 |

AI 도구 모음이 118개로 전체의 41%라 사이드바에서 독립시키고 하위를 접었다 폈다 합니다.
참고자료도 하위를 가집니다. 하위 카테고리는 관리자가 자유롭게 추가·수정·삭제합니다.

## 5. 기능 목록

### 공개 화면 (익명, 읽기 전용)

| # | 기능 | 설명 |
|---|---|---|
| P1 | 홈 | 즐겨찾기 세 묶음(AI 소식·AI 서비스·업무용 서비스) + 현재 운영 중인 사이트를 사각 카드 그리드로 |
| P2 | 사이드바 | 빠른 접근(홈·내 즐겨찾기·운영 중) / 분류(카테고리 10개, 하위 접기·펼치기) |
| P3 | 카테고리 목록 | 해당 분류 전체를 **홈과 동일한 사각 카드 그리드**로. 하위 탭으로 좁히기 |
| P4 | 키워드 검색 | ⌘K 팔레트. 이름·설명·태그·분류·주소 전부 대상. 입력 즉시 필터 |
| P5 | AI 의미 검색 | "AI 검색" 버튼 또는 ⌘↵. 이름이 안 겹쳐도 하는 일이 맞으면 가져오고, 선정 근거를 한 줄로 표시 |
| P6 | 키보드 이동 | ⌘K 열기 → ↑↓ 이동 → ↵ 새 탭 열기 → esc 닫기 |
| P7 | 한 번에 열기 | 섹션·카테고리 단위로 전체 열기 / 체크한 것만 열기 |
| P8 | 클릭 수 표시 | 카드 오른쌄 아래, **눈 아이콘 + 숫자**로 누적 클릭 표시 (0회도 `0`으로 표기) |
| P9 | 빈 상태 | 링크 0개 / 검색 결과 0건 / 즐겨찾기 0개 |
| P10 | **공용 즐겨찾기** | 관리자 화면의 카드 오른쪽 위 **핀** 토글. 담은 링크는 홈의 세 묶음과 “내 즐겨찾기” 목록에 모이며, 서버에 저장되어 모든 방문자·PC에 동일하게 보입니다. 사이드바·모바일 칩에도 진입점을 둡니다 |
| P11 | **관리자 현장 편집** | 관리자로 로그인한 상태로 공개 화면을 볼 때만 카드에 **연필·휴지통**이 보입니다. 연필 = 그 자리에서 이름·설명 인라인 수정, 휴지통 = 카드 위 확인 후 삭제. 비로그인 방문자에게는 렌더되지 않습니다 |

### 관리자 화면 (`/admin`, 인증 필요)

| # | 기능 | 설명 |
|---|---|---|
| A1 | 로그인 | 이메일 + 비밀번호(Supabase Auth). 실패 사유를 이메일/비밀번호로 나눠 알리지 않음 |
| A2 | 카테고리 · 링크 | 2단. 좌: 상위 카테고리 CRUD + 드래그 정렬 / 우: 선택 카테고리의 하위 CRUD + 링크 관리 |
| A3 | 링크 추가 | URL 붙여넣기 → 이름 자동 추출(비우면 도메인에서) → 선택된 카테고리로 등록 |
| A4 | 링크 편집 | 설명 인라인 수정, 하위 카테고리 지정, 드래그로 순서 변경. 공개 화면의 관리자 편집 모드에서는 공용 즐겨찾기 핀도 관리 |
| A5 | 필터·정렬 | 목록 내 검색 / 하위 칩 필터(전체·각 하위·하위 미지정) / 정렬 4종(직접 지정한 순서·하위 카테고리순·클릭 많은순·이름순) |
| A6 | 통계 | 누적 클릭, 오늘 클릭, 미사용 링크 수, 기간별 추이(14·30·90·180·365일), 링크 순위, 카테고리별 합계, 최근 클릭 로그 |
| A7 | 정리 도구 | 주소가 완전히 같은 중복 / 같은 도메인·다른 페이지(정리 대상 아님) / 오래 손대지 않은 링크(30·90·180·365일 기준) |

## 6. 클릭 집계와 어뷰징 방어

수집: 링크를 열기 직전에 비동기로 `clicks`에 1건 insert. 페이지 이동을 막지 않습니다.

방어 규칙 (프로토타입에 1·2 구현됨)
1. **쿨다운**: 같은 방문자·같은 링크는 30초 안에 재클릭해도 세지 않습니다.
2. **일일 상한**: 같은 방문자·같은 링크 하루 10회까지만 집계합니다.
3. **방문자 익명 ID**: 로컬 스토리지 UUID + IP 해시를 함께 저장해 "총 클릭"과 "사람 수(unique)"를 분리 표시합니다.
4. **서버 rate limit**: Edge Function에서 IP당 분당 30회 초과 무시.
5. **일괄 열기 구분**: "한 번에 열기"는 `bulk` 플래그로 기록해 순위 왜곡을 막습니다.

통계 화면에서 **순위는 unique 기준, 절대값은 참고용**으로 안내합니다.

## 7. 데이터 모델 (Supabase / Postgres)

```sql
categories (
  id            uuid primary key default gen_random_uuid(),
  name          text not null unique,
  parent_id     uuid references categories(id) on delete set null,  -- 하위 카테고리
  sort_order    int  not null default 0,
  created_at    timestamptz default now()
);

bookmarks (
  id            uuid primary key default gen_random_uuid(),
  category_id   uuid references categories(id) on delete set null,  -- 상위 또는 하위 id
  title         text not null,
  url           text not null,
  description   text,
  tags          text[] default '{}',
  favicon_url   text,
  is_pinned     boolean not null default false,   -- 제거된 매일 고정 기능의 레거시 컬럼
  is_favorite   boolean not null default false,   -- 공용 즐겨찾기 여부
  fav_order     int not null default 0,            -- 공용 즐겨찾기 안의 순서
  sort_order    int not null default 0,
  created_at    timestamptz default now(),
  updated_at    timestamptz default now()
);

-- 공용 즐겨찾기(P10)는 bookmarks.is_favorite/fav_order로 관리합니다.
-- 향후 사용자별 계정 즐겨찾기가 필요해지면 별도 favorites(user_id, bookmark_id, ...) 테이블로 승격합니다.

clicks (
  id            bigserial primary key,
  bookmark_id   uuid references bookmarks(id) on delete cascade,
  visitor_hash  text,          -- 익명 방문자 식별 (UUID + IP 해시)
  is_bulk       boolean default false,
  clicked_at    timestamptz default now()
);
create index on clicks (bookmark_id, clicked_at desc);
create index on clicks (clicked_at desc);
```

### RLS
- `categories`, `bookmarks`: 익명 `select` 허용. `insert/update/delete`는 인증된 관리자만.
- `clicks`: 익명 `insert`만 허용, `select`는 관리자만.
- 고정 최대 12개는 트리거 또는 애플리케이션 레벨에서 강제합니다.

### 데이터 이관
`data/links.json`의 290건을 그대로 seed로 씁니다. 필드 매핑:

| JSON | DB |
|---|---|
| `title` | `bookmarks.title` |
| `url` | `bookmarks.url` (utm·gclid 등 추적 파라미터 제거 완료) |
| `desc` | `bookmarks.description` |
| `group` + `sub` | `categories` 2계층으로 생성 후 `category_id` |
| `tags` | `bookmarks.tags` |
| `pinned` | `bookmarks.is_pinned` |
| `order` | `bookmarks.sort_order` |
| `added` | `bookmarks.created_at` (unix seconds) |
| `data/icons/<id>.png` | `bookmarks.favicon_url` (스토리지 업로드 후 URL) |

## 8. AI 의미 검색

키워드 검색으로 못 찾는 문장형 질의("휴가 어떻게 쓰는지")를 처리합니다.

- 트리거: "AI 검색" 버튼 클릭 또는 ⌘↵ (매 타자마다 호출하지 않습니다)
- 입력: 질의문 + 전체 링크의 `title/description/tags/category` 요약
- 출력: 링크 3~5개 + **각각 왜 골랐는지 한 줄 근거**
- 결과는 키워드 결과와 시각적으로 분리하고 소요 시간을 표시합니다
- 구현: pgvector 임베딩 검색 또는 LLM 호출 중 선택. 프로토타입은 의도 사전(10종) + 토큰 겹침 폴백으로 흉내냅니다

## 9. 개발 단계

1. **1단계 — 공개 화면 읽기 전용**: 스키마 + seed + 홈 + 사이드바 + 카테고리 목록 + 파비콘. 클릭 집계 포함
2. **2단계 — 검색**: ⌘K 팔레트, 키보드 이동, 한 번에 열기
3. **3단계 — 관리자**: 로그인, 카테고리·링크 CRUD, 드래그 정렬, 고정 관리
4. **4단계 — 통계·정리**: 클릭 집계 대시보드, 어뷰징 방어, 정리 도구
5. **5단계 — AI 검색**: 임베딩 또는 LLM 연동

## 10. 이번 범위에서 제외

- 즐겨찾기의 기기 간 동기화 (계정이 없으므로 브라우저 단위 저장까지)
- 사용자 계정·권한(관리자 계정 1~2개만)
- 크롬 탭 그룹으로 묶어 열기 — 웹 권한 밖입니다. 필요하면 크롬 확장을 별도로 만들어야 하며, 확장 없이는 여러 탭 열기까지만 가능합니다
- 링크 유효성 자동 검사(죽은 링크 탐지)
